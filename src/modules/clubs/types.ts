export type ClubActivity = {
  club: string;
  date: string;
  location: string;
  mood: string;
  seats: string;
  title: string;
};

export type ClubOpportunity = {
  body: string;
  label: string;
  metric: string;
};

export type ClubSignal = {
  label: string;
  value: string;
};
